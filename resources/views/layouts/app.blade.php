<!doctype html>
<html lang="en">

<head>

    @include('partials.head')

</head>

<body>

    <!-- WRAPPER
=====================================================================================================================-->
    <div class="ts-page-wrapper ts-{{ request()->is('/') ? 'homepage' : 'has-bokeh-bg' }}" id="page-top">

        @include('partials.header')


        @if (request()->is('/'))
            @include('partials.hero-map')
        @endif


        <!--*********************************************************************************************************-->
        <!-- MAIN ***************************************************************************************************-->
        <!--*********************************************************************************************************-->

        <main id="ts-main">

            @yield('content')

        </main>

        @include('partials.footer')
